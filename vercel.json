{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && DATABASE_URL=$POSTGRES_PRISMA_URL npx prisma db push --accept-data-loss && next build",
  "crons": [
    {
      "path": "/api/cron/renew-gmail-watch",
      "schedule": "0 6 * * *"
    }
  ]
}
